/** Feature service surface for the corresponding desktop bridge routes. */
export {
  getSessionInfo,
  getSystemStats,
  getTopProcesses,
  readJsonBody,
  sendJson,
} from './bridgeServiceRuntime.js';
